@{
    // use to check the status and then
        // if yoy toggile the
    var status = Context.Request.Query["status"];

    var pending = "";
    var approved = "";
    var completed = "";
    var checkIn = "";
    var canceled = "";

    switch (status)
    {
        case SD.StatusPending:
            pending = "text-white bg-success";
            break;

        case SD.StatusApproved:
            approved = "text-white bg-success";
            break;
        case SD.StatusCompleted:
            completed = "text-white bg-success";
            break;
        case SD.StatusCheckIn:
            checkIn = "text-white bg-success";
            break;
        case SD.StatusCancelled:
            canceled = "text-white bg-success";
            break;

    }
}



<div class="w-100 card border-0 p-4">
    <div class="card-header bg-success bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Booking List</h2>
            </div>
        </div>
    </div>

    <div class="card-body border p-4">
        <div class="row pb-3">
       
        </div>
        <table id="tblBookings" class="table table-bordered table-striped">

            <div class="d-flex justify-content-end pb-4 pt-2">
                <ul class="list-group list-group-horizontal-sm">
                    <a style="text-decoration:none;" asp-controller="Booking" asp-action="Index"
                        asp-route-status="@SD.StatusPending">
                        <li class="list-group-item @pending"> @SD.StatusPending </li>
                    </a>

                    <a style="text-decoration:none;" asp-controller="Booking" asp-action="Index"
                    asp-route-status="@SD.StatusApproved">
                    <li class="list-group-item @approved"> @SD.StatusApproved </li>
                    </a>
                    <a style="text-decoration:none;" asp-controller="Booking" asp-action="Index"
                    asp-route-status="@SD.StatusCheckIn">
                    <li class="list-group-item @checkIn"> @SD.StatusCheckIn </li>
                    </a>  
                    <a style="text-decoration:none;" asp-controller="Booking" asp-action="Index"
                        asp-route-status="@SD.StatusCompleted">
                        <li class="list-group-item @completed"> @SD.StatusCompleted </li>
                    </a>  
                    <a style="text-decoration:none;" asp-controller="Booking" asp-action="Index"
                        asp-route-status="@SD.StatusCancelled">
                        <li class="list-group-item @canceled"> @SD.StatusCancelled </li>
                    </a>
                </ul>
            </div>

            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Phone</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Check In Date</th>
                    <th>Nights</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
               @*  @foreach (var item in Model)
                {
                                <tr>
                                    <td>@item.Villa.Name</td>
                                    <td>@item.Name</td>
                                    <td>@item.Description</td>
                                    <td>
                                        <div  class="w-75 btn-group" role="group" > 

                                                    <a asp-action="Update" asp-controller="Amenity" asp-route-ameityId="@item.Id"
                                                    class="btn btn-success mx-2">
                                                    <i class="bi bi-pencil-square"></i>
                                                    Edit
                                                </a>


                                                    <a asp-action="Delete" asp-controller="Amenity" asp-route-villaId="@item.Id"
                                                    class="btn btn-danger mx-2">
                                                        <i class="bi bi-trash-fill"></i>   Delete

                                                </a>
                                        </div>
                                    </td>
                                </tr>
                } *@
            </tbody>
        </table>
    </div>
</div>
@section scripts{
    <script src="~/js/Booking.js"></script>
}